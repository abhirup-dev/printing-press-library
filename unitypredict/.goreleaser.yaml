version: 2
project_name: unitypredict-pp-cli
changelog:
  disable: true
builds:
  - id: unitypredict-pp-cli
    main: ./cmd/unitypredict-pp-cli
    binary: unitypredict-pp-cli
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X unitypredict-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: unitypredict-pp-mcp
    main: ./cmd/unitypredict-pp-mcp
    binary: unitypredict-pp-mcp
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: unitypredict-pp-cli
    repository:
      owner: dev-abhirup-sc
      name: homebrew-tap
    homepage: "https://github.com/dev-abhirup-sc/unitypredict-pp-cli"
    description: "Discovered API spec for dev-unitypredict"
    install: |
      bin.install "unitypredict-pp-cli"
      bin.install "unitypredict-pp-mcp"
