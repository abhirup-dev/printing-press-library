.PHONY: build test lint install clean

BIN_EXT := $(if $(filter windows,$(shell go env GOOS)),.exe,)

build:
	go build -trimpath -o bin/chatgpt-pp-cli$(BIN_EXT) ./cmd/chatgpt-pp-cli

test:
	go test ./...

lint:
	golangci-lint run

install:
	go install ./cmd/chatgpt-pp-cli

clean:
	rm -rf bin/

build-mcp:
	go build -trimpath -o bin/chatgpt-pp-mcp$(BIN_EXT) ./cmd/chatgpt-pp-mcp

install-mcp:
	go install ./cmd/chatgpt-pp-mcp

build-all: build build-mcp
